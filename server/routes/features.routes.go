package routes

import (
	"github.com/gofiber/fiber/v2"
)

func RegisterFeaturesRoutes(router fiber.Router) {
	// feature := router.Group("/feature")
	// feature.Post("/url-shortener",middlewares.ValidateBody[]())
}
